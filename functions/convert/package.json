{
  "name": "convert",
  "version": "1.0.0",
  "description": "native modules required by lambda",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=1 ~/.yarn/bin/yarn"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^0.12.0"
  }
}
